---
title: "MCP Sampling: Letting a Server Borrow the Client's LLM"
track: "mcp"
status: live
summary: "A server asks the connected client to run a completion on its behalf, instead of holding its own model or API key."
duration: "5 min read"
published: "2026-09-15"
updated: "2026-09-15"
---

import Callout from '../../../components/kit/Callout.astro';
import InlineCheck from '../../../components/kit/InlineCheck.astro';
import Steps from '../../../components/kit/Steps.astro';
import Step from '../../../components/kit/Step.astro';

Most MCP servers are dumb pipes: the client's LLM calls a tool, the tool runs code, the result goes back. Sampling flips that. It lets the *server* ask the client to run an LLM completion for it, mid-task, and hand the result back to the server.

## Why a server would want this

Imagine an MCP server that summarizes long documents before returning them, or that triages a batch of support tickets and only surfaces the three that need a human. Without sampling, that server needs its own API key, its own provider account, and its own bill. With sampling, it sends a `sampling/createMessage` request back over the same connection, and the client's already-configured model does the work. One API key for the whole session, held by the client, not scattered across every server it talks to.

This matters more as you connect several servers at once — see [Orchestrating Multiple MCP Servers in One Client Session](/learn/mcp/multi-server-orchestration). Without sampling, each server that wants intelligence needs its own model access. With it, they all borrow the one the user already trusts and pays for.

## How the request actually flows

Sampling runs opposite the usual direction. Normally: client to server. Sampling: server to client, mid-tool-call.

<Steps>
<Step title="Client calls a tool">
An ordinary `tools/call` — nothing special yet.
</Step>
<Step title="Server asks to sample">
Mid-handler, the server sends `sampling/createMessage` back to the client: messages, a system prompt, and model *preferences* — hints, not a hard model name.
</Step>
<Step title="Client checkpoints with a human">
The client shows the user what's being asked (usually), gets approval, and runs the completion with its own model.
</Step>
<Step title="Completion returns to the server">
The client sends the text back as the sampling response.
</Step>
<Step title="Tool call resumes">
The server finishes the original call using that result — without ever holding an API key.
</Step>
</Steps>

The server never sees the client's API key and never picks the exact model — it can express preferences (favor speed, favor a specific vendor's family) but the client makes the final call. That's deliberate: the client controls cost and which model actually runs.

## The trust boundary is the whole point

Because a server can now trigger LLM calls, a compliant client is expected to put a human in the loop — showing the prompt the server wants sent, and often the completion, before it's used. This is the same instinct that runs through [MCP Elicitation: Servers Asking the User Mid-Session](/learn/mcp/mcp-elicitation), where a server pauses to ask the user something directly. Sampling and elicitation are siblings: both let a server reach back into the client session instead of returning inert data, and both hand a human (or at least the client) a checkpoint to say no.

Client support for sampling is optional, and as of 2026 it's still the least commonly implemented capability among MCP clients — many stick to tools and resources only. A server that depends on sampling should degrade gracefully, or state the requirement clearly, when the connected client doesn't advertise the capability.

<InlineCheck
  question="Why does the server send model *preferences* rather than naming the exact model to use?"
  options={['The client owns the final call — it controls the API key, the cost, and which model actually runs; the server can only hint at speed or vendor family', 'Because model names change between providers', 'Because the server lacks permission to see model names']}
  answer={0}
  explanation="Sampling keeps the trust boundary intact: the server never sees the client's API key and never picks the model. It expresses preferences; the client decides — that's what keeps cost and capability under the party that pays for them."
/>

**Related:** [MCP Elicitation: Servers Asking the User Mid-Session](/learn/mcp/mcp-elicitation), [MCP Tools, Resources, and Prompts](/learn/mcp/mcp-tools-resources-and-prompts), [Orchestrating Multiple MCP Servers in One Client Session](/learn/mcp/multi-server-orchestration)
